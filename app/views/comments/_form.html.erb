<%= form_with(model:[board,comment], local: true) do |f| %>
  <%= f.label Comment.model_name.human %>
  <%= f.text_area :body, class: "form-control mb-3", row:"4", placeholder: "#{t('defaults.placeholder.comment')}" %>
  <%= f.submit t('defaults.post'), class: 'btn btn-primary' %>
<% end %>

