<%= link_to bookmark_path(board.bookmarks.find { |b| b.user_id == current_user.id }), method: :delete, class: "float-right", id: "js-bookmark-button-for-board-id-#{ board.id }", remote: true do %>
  <%= icon 'fas', 'star' %>
<% end %>
